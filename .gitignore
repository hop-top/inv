# OS files
.DS_Store
Thumbs.db
*.swp
*~

# Editor/IDE
.idea/
.vscode/

# Environment
.env
.env.local
.env.*.local
# Rust
target/
# Cargo.lock: commit for bins (this template ships a CLI), ignore for libs
*.rs.bk
**/*.rs.bk
*.pdb

# Per-task worktrees (sibling layout intended, but accept nested for ergonomics)
inv-T-*/
# Symlinks added so nested worktrees can resolve ../kit and ../uri-poly path deps
/kit
/uri-poly
