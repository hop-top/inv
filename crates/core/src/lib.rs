//! hop-top-inv-core — domain types, FSM facade, tax engine, render pipeline,
//! and the one-command-core layer that every channel adapter calls into.

pub mod domain;
pub mod render;
pub mod state;
pub mod tax;
