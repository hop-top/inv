//! inv-core — domain types, FSM facade, tax engine, render pipeline,
//! and the one-command-core layer that every channel adapter calls into.
//!
//! Modules are populated by later tasks (T-0004 through T-0013). This file
//! exists at T-0002 so `cargo check --workspace` is green.
