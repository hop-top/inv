[package]
name = "hop-top-inv-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "inv: CLI channel adapter (clap)"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
chrono = { workspace = true }
hop-top-kit = { workspace = true }
rust_decimal = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

hop-top-inv-bus = { workspace = true }
hop-top-inv-commands = { workspace = true }
hop-top-inv-core = { workspace = true }
hop-top-inv-store = { workspace = true }

# `inv server` composition: the cli subcommand boots HTTP+WS+MCP and the
# tickers + outbox in a single tokio runtime. These are server-only deps;
# all the other inv subcommands stay light.
hop-top-inv-api = { workspace = true }
hop-top-inv-mcp = { workspace = true }
hop-top-inv-ws = { workspace = true }
axum = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
