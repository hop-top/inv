[package]
name = "inv-cli"
version.workspace = true
edition.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "inv: CLI channel adapter (clap)"

[dependencies]
anyhow = { workspace = true }
clap = { workspace = true }
chrono = { workspace = true }
hop-top-kit = { workspace = true }
rust_decimal = { workspace = true }
serde = { workspace = true }
serde_json = { workspace = true }
tokio = { workspace = true }
toml = { workspace = true }
tracing = { workspace = true }
tracing-subscriber = { workspace = true }

inv-bus = { workspace = true }
inv-commands = { workspace = true }
inv-core = { workspace = true }
inv-store = { workspace = true }

# `inv server` composition: the cli subcommand boots HTTP+WS+MCP and the
# tickers + outbox in a single tokio runtime. These are server-only deps;
# all the other inv subcommands stay light.
inv-api = { workspace = true }
inv-mcp = { workspace = true }
inv-ws = { workspace = true }
axum = { workspace = true }

[dev-dependencies]
tempfile = { workspace = true }

[lints]
workspace = true
