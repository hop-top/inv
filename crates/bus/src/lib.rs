//! inv-bus — bus event types, publisher/consumer adapters, transactional
//! outbox relay, idempotent inbox.
//!
//! Wraps kit's Rust bus primitive once it ships (tracked by
//! `hop-top/poly-kit#sdk-rs-runtime`); facade-then-swap until then.
//!
//! Populated by T-0014 and T-0015.
